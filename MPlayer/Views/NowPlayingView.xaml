<UserControl x:Class="MPlayer.Views.NowPlayingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1366">
    <Grid Background="#1E1E1E">
        <ContentControl x:Name="PlayQueueViewModel" VerticalAlignment="Bottom" HorizontalAlignment="Right"
                        Margin="0,0,32,128"/>
        
        <ContentControl x:Name="PlaybackControlsViewModel" VerticalAlignment="Bottom" HorizontalAlignment="Right"
                        Margin="0,0,16,16" Width="200"/>
    </Grid>
</UserControl>
